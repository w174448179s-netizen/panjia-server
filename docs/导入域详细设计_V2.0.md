# 盘家智管 · 导入域（panjia-import）· 详细设计

> **版本：** V2.0（单据域回归版 · 落地版）
> **编制日期：** 2026 年 9 月
> **定位：** 基于《薪酬与收支架构设计 V1.8》§2.3/§4.2/§11.1 +《业务需求说明书 V4.2》+《AI 总规约 V3.0》，将 `panjia-import` 落地为可直接编码的详细设计。
> **V2.0 核心变更（回归 V1.8 基线）：** 本域**仅负责业务单据**（业绩 / 考勤 / 积分 / 费用），**移除 `EMPLOYEE` source_type 与员工主数据导入**。员工导入回归 `panjia-people` 本域（见《员工域详细设计 V6.0》）。本域对 people 域**保持仅只读依赖**（`EmployeeQueryPort`），不再有任何受控写。文件解析/模板解析/归档/基础格式校验下沉到 `common-import-util` 工具层，本域只做单据业务。
> **前置条件：** 底座（RuoYi-Vue-Plus 6.x）、`panjia-contracts`、`panjia-people`（V6.0）、`common-import-util` 已完成。
> **依赖边界：** 依赖 people（**只读** `EmployeeQueryPort`）+ `common-import-util`（工具层）；被 `panjia-performance` 依赖（提供 `NormalizedRecord`）。
> **读者：** 编码负责人、实施顾问、performance / people 域开发者。

---

## 〇、设计目标与边界（一句话对齐）

导入域负责 **`业务单据 Excel → RawData（只读归档）→ NormalizedRecord（标准化）`**；**不构造业务事实、不理解业绩/薪酬语义、不做员工主数据导入**。

贝壳模板/格式变更**只影响本域内部**（新增/调整 DataSource 实现），不波及其他域——这是本域独立的全部价值。

### 0.1 与员工导入的边界（★ V2.0 关键）

| 导入类型 | 归属 | 本域是否负责 |
|---------|------|:----------:|
| 业绩（贝壳结佣/新签） | `panjia-import` | ✅ |
| 考勤 / 积分 / 费用（OTHERS） | `panjia-import` | ✅ |
| **员工主数据（Excel）** | **`panjia-people`** | ❌ **本域不负责** |

**两套导入的复用关系：**

```
                ┌──────────────────────┐
                │  common-import-util  │  工具层：解析/模板/归档/基础校验
                └──────────┬───────────┘
                           │ 仅工具层复用
              ┌────────────┴────────────┐
              ▼                         ▼
      ┌───────────────┐        ┌────────────────┐
      │ panjia-import │        │ panjia-people  │
      │  业务单据      │        │  员工主数据     │
      │ 批次/Raw/NR   │        │ 批次/Raw/Issue │
      │ 单据状态机     │        │ 员工导入状态机  │
      └───────────────┘        └────────────────┘
         业务层不复用              业务层不复用
```

> **仅工具层复用，业务层不复用** —— 主数据（不可物理删、需演进）与单据（可逻辑失效）语义不同，强行复用会混杂；保护 V1.8 基线 ADR-007 / ADR-021 / ADR-022。

### 0.2 三不原则

| 环节 | 职责 | 禁止 |
|---|---|---|
| 导入域 | 单据 Excel → RawData → NormalizedRecord | 构造 PerformanceFact / 员工业务对象 / 理解业绩语义 |
| 业绩域 | 消费 NormalizedRecord → PerformanceFact | 解析 Excel / 依赖数据源格式 |
| 员工域 | 员工 Excel → Employee/salary_fact（**本域自建**） | 调用本域业务接口 |
| 工具层 | 文件解析/模板/归档/基础校验 | 落库 / 业务语义校验 |
| 导入域金额 | 只做**类型转换**，原值透传 | 业绩折算、分成、金额二次加工 |

---

## 一、数据源与模板全景（5 类，均为业务单据）

| 编码（source_type） | 中文名 | 文件/来源 | 归一化产物 | 对应下游业务 |
|---|---|---|---|---|
| `KE_SIGNED` | 贝壳·理房通到账明细（结佣） | 贝壳后台 Excel | `NormalizedRecord`（SIGNED） | 经纪人/店长/总监个人结佣提成 |
| `KE_NEW_SIGN` | 贝壳·新签业绩明细（应收） | 贝壳后台 Excel | `NormalizedRecord`（NEW_SIGN） | 店长团队提成、保底、总监门店提成 |
| `ATTENDANCE` | 考勤 Excel | 人事整理标准模板 | `NormalizedRecord`（ATTENDANCE） | 考勤扣款 |
| `POINTS` | 积分 Excel | 人事整理标准模板 | `NormalizedRecord`（POINTS） | 积分扣款 + 绩效等级（A/B/C） |
| `OTHERS` | 手工录入/其他费用 | 系统手工录入兜底 | `NormalizedRecord`（MANUAL） | 商业保险、宿舍管理、其他支出 |

> **重要口径（V4.2 §7.1、§9.1）：**
> - **结佣**（实收）走 `KE_SIGNED`；**新签**（应收）走 `KE_NEW_SIGN`；两者**同源但语义不同**，必须拆成两个 DataSource，**绝不可混为一张"业绩表"**。
> - 考勤/积分**不对接钉钉**（V4.2 修订说明第7条），由人事整理为标准模板导入。
> - **员工主数据不属于本域**（V2.0 移除 `EMPLOYEE`）。

### 1.1 贝壳到账明细关键字段（RawData 原始列）

| 原始列（raw） | 类型 | 说明 | 归一化映射 |
|---|---|---|---|
| `arrive_month` | varchar | 到账月 | `period`（YYYY-MM） |
| `biz_type` | varchar | 业务类型（一手房/二手买卖/租赁/...） | `biz_type` |
| `order_no` | varchar | 订单号（一手房/房产金融/家装荐客关联键） | `source_key`（之一） |
| `contract_no` | varchar | 合同号（二手买卖/租赁/轻托管等关联键） | `source_key`（之一） |
| `role_sys_no` | varchar | 角色人系统号（经纪人唯一标识） | `employee_external_code` → 匹配 Employee |
| `role_name` | varchar | 角色人姓名 | `employee_name`（校验用） |
| `role_type` | varchar | 角色类型（客源成交人/房源维护人/...） | `role_type` |
| `share_ratio` | numeric | 业绩比例 | `share_ratio` |
| `current_receivable` | numeric(18,2) | 当月应收业绩 | `receivable_amount`（原值） |
| `current_received` | numeric(18,2) | 当月实收业绩 | `received_amount`（原值） |

> RawData **原样存原始列名与原始值**，仅做类型规整；业务语义解析下沉到 Normalize 阶段，由模板配置驱动——贝壳改列名只改模板。

### 1.2 手工录入兜底（OTHERS）

无 Excel 时由算薪人员逐条录入（商业保险费、宿舍管理费、其他支出等）。不产 RawData 文件，仍生成 `NormalizedRecord`（`raw_data_id = null`），受同一批次与校验约束。**强制走 `AuditPort`** 留痕。

**去重为编码硬约束**：判重键 `(batch_id, employee_external_code, period, item_type)`；插入前先 SELECT，命中抛 `BizException`；跨批允许共存。规则在 DO 字段注释、`uk_norm_source_key` SQL 注释、本文档三处同步。

### 1.3 关联键 `source_key` 规则骨架

由 `biz_type` 决定：一手房/房产金融/家装荐客用**订单号**；二手买卖/租赁/轻托管用**合同号**；同一合同多角色拼接 `contract_no + role_type` 保证行级唯一。

> **待确认**：完整映射（租赁轻托管/写字楼租赁等细分）**待贝壳真实 Excel 样本到位后固化**（§9.2-1）。在此之前 `SourceKeyGenerator` 以接口契约先行，规则实现可注入。

---

## 二、导入数据链路

```
  贝壳/人事 Excel        标准模板下载
       │                      │
       ▼                      ▼
  ┌────────────────┐   ┌──────────────────┐
  │ common-import-  │   │ TemplateEngine    │  工具层：解析/模板/归档/基础校验
  │ util（工具层）   │   │ (pj_import_       │  业务层：模板定义持久化在本域
  └───────┬────────┘   │  template)        │
          │ ParsedSheet └────────┬─────────┘
          ▼                      │ 列映射
  ┌────────────┐                 │
  │  RawData    │◀────────────────┘  只读归档（raw_json 保留原始值）
  └─────┬──────┘
        │ Normalize（列映射 + 类型转换 + 员工匹配 + 基础校验）
        ▼
  ┌──────────────────┐  校验失败 → ImportIssue
  │ NormalizedRecord  │
  └─────┬────────────┘
        ▼
  panjia-performance 消费 → PerformanceFact
```

---

## 三、领域对象设计

### 3.1 聚合根：`ImportBatch`（一次导入的批次）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | Long（雪花） | 主键 |
| `batchNo` | varchar(32) | 批次号（`IMP + yyyyMMdd + 序列`） |
| `sourceType` | varchar(20) | 数据源类型（**5 类，无 EMPLOYEE**） |
| `templateVersion` | varchar(20) | **创建/解析时快照冻结** |
| `fileName` / `originalFileName` | varchar | 存储文件名 / 原名 |
| `storagePath` | varchar | 归档路径（工具层 `FileArchiver` 返回） |
| `period` | varchar(7) | 归属月（YYYY-MM），唯一性核心维度 |
| `totalRows` / `successRows` / `failedRows` | int | 行数统计 |
| `status` | smallint | 状态机（§3.5） |
| `archiveStatus` | smallint | 归档状态 |
| `operatorId` / `deptId` | Long | 操作人 / 归属部门 |
| `version` | int | 乐观锁 |
| `supersededByBatchId` | Long | 被新批次废弃后回填，指向新批次 |

**关键不变量：**
- `source_type + period + dept_id` 决定业务唯一性；重复导入走"归档旧 + 新建"（§5.5）。
- `template_version` **创建/解析时**快照冻结（非归档时），避免解析逻辑与模板版本不一致。
- `superseded_by_batch_id` 仅由重复导入流程在新批次归档成功**后**回填，一经设置不可改。

### 3.2 实体：`RawData`（原始记录 · 只读）

**只追加、不可 UPDATE/DELETE**（应用层 insert-only + AuditPort；**禁止触发器**）。按 source_type 分表：

| 表 | 承载 |
|---|---|
| `pj_import_raw_signed` | KE_SIGNED 结佣明细 |
| `pj_import_raw_new_sign` | KE_NEW_SIGN 新签明细 |
| `pj_import_raw_attendance` | 考勤 |
| `pj_import_raw_points` | 积分 |
| `pj_import_raw_manual` | 手工录入 |

公共列：`id, batch_id, row_no, raw_json(jsonb), create_time`。**所有原始列允许 NULL（原样保留）**。

> `raw_json` 全量冗余存储，是审计与重归一化的锚点。**V2.0 移除 `pj_import_raw_employee`** —— 员工原始归档由 people 域 `pj_people_import_raw` 承载。

### 3.3 实体：`NormalizedRecord`（归一化记录）

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` | Long（雪花） | 主键 |
| `batchId` | Long | 所属批次 |
| `recordType` | varchar(20) | SIGNED / NEW_SIGN / ATTENDANCE / POINTS / MANUAL |
| `period` | varchar(7) | 归属月 |
| `employeeId` | Long | 关联 Employee.id；匹配不到为 null + ImportIssue |
| `employeeExternalCode` | varchar | 外部编码（角色人系统号/工号） |
| `sourceKey` | varchar | 业务唯一键（订单号/合同号/考勤日期） |
| `bizType` | varchar | 业务类型 |
| `receivableAmount` | numeric(18,2) | 应收业绩（原值） |
| `receivedAmount` | numeric(18,2) | 实收业绩（原值） |
| `shareRatio` | numeric(10,4) | 业绩比例 |
| `roleType` | varchar | 角色类型 |
| `extraJson` | jsonb | 扩展字段 |
| `rawDataId` | Long | 对应 RawData 行（手工录入为 null） |
| `validationStatus` / `validationMsg` | smallint / varchar | 校验状态与原因 |

> **不含业务语义字段**（提点/提成金额），只有结构化数据 + 关联键 + 金额原值。

### 3.4 值对象：`ImportIssue`

| 字段 | 类型 | 说明 |
|---|---|---|
| `id` / `batchId` / `rowNo` | Long / Long / int | |
| `issueType` | varchar(20) | `EMPLOYEE_NOT_MATCH` / `COLUMN_TYPE_ERR` / `REQUIRED_MISSING` / `DUPLICATE_KEY` / `PERIOD_MISMATCH` |
| `fieldName` / `rawValue` / `message` | varchar | |
| `status` | smallint | `OPEN / RESOLVED / IGNORED` |

- `RESOLVED`：已人工修复数据（补齐员工档案后重归一化）；
- `IGNORED`：业务确认可忽略，**仅限算薪人员/总监**标记，Controller 鉴权 + AuditPort 留痕。

> **V2.0 移除** `DEPT_NOT_MATCH` / `POST_NOT_MATCH`（属员工导入，归 people 域）。

### 3.5 状态机：`ImportBatchStatus`

```
  PARSING ──▶ NORMALIZING ──▶ PENDING_CONFIRM ──▶ ARCHIVED
                                  │
                                  └──▶ FAILED（终态，可新建批次重试）
```

| code | desc |
|---|---|
| `PARSING` | 解析中（读取文件、落 RawData） |
| `NORMALIZING` | 归一化中 |
| `PENDING_CONFIRM` | 待确认（有 ImportIssue） |
| `ARCHIVED` | 已归档（对外可见/可被消费） |
| `FAILED` | 失败（终态） |

```java
public enum ImportBatchStatus {
    PARSING(0, "解析中", Set.of(NORMALIZING, FAILED), false, true),
    NORMALIZING(1, "归一化中", Set.of(PENDING_CONFIRM, FAILED), false, true),
    PENDING_CONFIRM(2, "待确认", Set.of(NORMALIZING, ARCHIVED, FAILED), false, false),
    ARCHIVED(3, "已归档", Set.of(), true, false),
    FAILED(4, "失败", Set.of(), true, false);

    private final int code;
    private final String desc;
    private final Set<ImportBatchStatus> next;
    private final boolean terminal;
    private final boolean running;

    public boolean canTransitTo(ImportBatchStatus t) { return next.contains(t); }
    public boolean isTerminal() { return terminal; }
    public boolean isRunning()  { return running; }
}
```

**强制规则：**
1. `FAILED` 终态，只能新建批次重试（`canTransitTo` 恒 false，单测固化）；
2. **重归一化只重建派生数据**：RawData 完全不动，只重建 NormalizedRecord + ImportIssue；
3. 跨月检测在 `PARSING` 前置，命中即 `FAILED`。

---

## 四、包结构与依赖

```
panjia-import/
├── panjia-import-api        ← 对外 Port（NormalizedRecordQueryPort）
├── panjia-import-domain     ← ImportBatch / RawData / NormalizedRecord / 状态机 / DataSource SPI
│   ├── service/             ImportEngine（两段事务编排）
│   └── datasource/          KeSigned / KeNewSign / Attendance / Points / Others
├── panjia-import-infrastructure
│   ├── mapper/              MyBatis（含 SUPERSEDED 过滤）
│   ├── template/            TemplateEngine
│   └── util/                ★ 调用 common-import-util（FileParser / TemplateResolver / FileArchiver / BasicValidator）
└── panjia-import-app        ← ApplicationService + Controller
```

**依赖关系（V2.0 降级）：**

| 依赖 | 方向 | 说明 |
|------|------|------|
| `panjia-contracts` | → | 事件/Port 契约 |
| **`panjia-people-api`（只读）** | → | **仅 `EmployeeQueryPort`**（查员工/部门做匹配），**无任何写 Port** |
| `common-import-util` | → | 文件解析/模板解析/归档/基础校验（工具层） |
| **禁止** | ✗ | 引入 people `domain` 包；写任何 `pj_people_*` 表；`EmployeeImportSink` 等受控写（**V2.0 已移除**） |

**CI 校验：** 扫描 `panjia-import` 源码，断言**不出现** `pj_people_` 表名、`EmployeeImportSink`、`EmployeeWritePort`。

**DataSource 清单（5 类）：**

| source_type | DataSource 实现 |
|---|---|
| KE_SIGNED | `KeSignedDataSource` |
| KE_NEW_SIGN | `KeNewSignDataSource` |
| ATTENDANCE | `AttendanceDataSource` |
| POINTS | `PointsDataSource` |
| OTHERS | `ManualEntryDataSource` |

---

## 五、导入流程

### 5.1 通用主流程（两段式事务）

```
UploadFile / ManualEntry
  ↓
① 创建 ImportBatch（快照冻结 templateVersion）
  ↓
② 【事务 A】解析 + 分批落 RawData
   - 分批 BATCH_INSERT_SIZE = 300；单批上限 MAX_ROWS_PER_BATCH = 5000
   - 失败仅回滚当前子批次；残留 RawData 属审计保留
   - 跨月检测前置（命中 → FAILED）
  ↓
③ 【事务 B】归一化 → NormalizedRecord + ImportIssue
  ↓
④ 状态推进：全通过 → ARCHIVED；有 issue → PENDING_CONFIRM
```

### 5.2 归一化流水线

```
NormalizeService.normalize(batch)
  ↓
列映射（按 pj_import_template）→ 类型转换 → 员工匹配（external_code → Employee.id）
  → 生成 NormalizedRecord
  → 匹配失败 → ImportIssue(EMPLOYEE_NOT_MATCH)，employee_id = null
```

### 5.3 校验规则

- `REQUIRED_MISSING`：关键列缺失（employee_external_code / period / 金额）
- `COLUMN_TYPE_ERR`：类型转换失败
- `DUPLICATE_KEY`：`source_key` 重复；手工录入走业务判重键
- `PERIOD_MISMATCH`：同批次 `arrive_month` 不一致 → FAILED
- `EMPLOYEE_NOT_MATCH`：external_code 在 people 域查不到

### 5.4 重归一化

`PENDING_CONFIRM → NORMALIZING`：RawData 不动；先按 `batch_id` 删旧 NormalizedRecord + ImportIssue，再重新生成；全程 `AuditPort` 留痕。

### 5.5 重复导入与 SUPERSEDED（单据：逻辑失效）

新批次归档成功后回填旧批次 `superseded_by_batch_id`；**RawData 保留**，下游按 `archive_status = ARCHIVED AND superseded_by_batch_id IS NULL` 过滤。

> **与员工主数据的差异（V2.0 明确）**：单据走**逻辑失效**（保留审计）；员工主数据走**演进**（upsert + 变更入口，不物理删），由 people 域独立实现。两者策略不同，**业务层不复用**。

---

## 六、数据模型（Flyway 精选）

### 6.1 `pj_import_batch`

```sql
CREATE TABLE pj_import_batch (
    id                      BIGSERIAL PRIMARY KEY,
    batch_no                VARCHAR(32)  NOT NULL UNIQUE,
    source_type             VARCHAR(20)  NOT NULL,     -- 5 类，无 EMPLOYEE
    template_version        VARCHAR(20)  NOT NULL,
    file_name               VARCHAR(255),
    original_file_name      VARCHAR(255),
    storage_path            VARCHAR(500),
    period                  VARCHAR(7),
    total_rows              INT          NOT NULL DEFAULT 0,
    success_rows            INT          NOT NULL DEFAULT 0,
    failed_rows             INT          NOT NULL DEFAULT 0,
    status                  SMALLINT     NOT NULL DEFAULT 0,
    archive_status          SMALLINT,
    operator_id             BIGINT,
    dept_id                 BIGINT,
    remark                  VARCHAR(500),
    version                 INT          NOT NULL DEFAULT 0,
    superseded_by_batch_id  BIGINT       REFERENCES pj_import_batch(id),
    create_time             TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP,
    update_time             TIMESTAMP    NOT NULL DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX uk_import_batch_type_period_dept
    ON pj_import_batch(source_type, period, dept_id)
    WHERE superseded_by_batch_id IS NULL AND archive_status = 'ARCHIVED';
```

### 6.2 RawData 分表（5 张，无 employee）

```sql
CREATE TABLE pj_import_raw_signed     (LIKE pj_import_raw_template INCLUDING ALL);
CREATE TABLE pj_import_raw_new_sign   (LIKE pj_import_raw_template INCLUDING ALL);
CREATE TABLE pj_import_raw_attendance (LIKE pj_import_raw_template INCLUDING ALL);
CREATE TABLE pj_import_raw_points     (LIKE pj_import_raw_template INCLUDING ALL);
CREATE TABLE pj_import_raw_manual     (LIKE pj_import_raw_template INCLUDING ALL);
-- 公共结构：id, batch_id, row_no, raw_json(jsonb), create_time
```

### 6.3 `pj_import_issue`

```sql
CREATE TABLE pj_import_issue (
    id          BIGSERIAL PRIMARY KEY,
    batch_id    BIGINT       NOT NULL REFERENCES pj_import_batch(id),
    row_no      INT,
    issue_type  VARCHAR(20)  NOT NULL,
    field_name  VARCHAR(64),
    raw_value   VARCHAR(500),
    message     VARCHAR(1000),
    status      SMALLINT     NOT NULL DEFAULT 0   -- 0 OPEN / 1 RESOLVED / 2 IGNORED
);
```

### 6.4 `pj_normalized_record`

```sql
CREATE TABLE pj_normalized_record (
    id                      BIGSERIAL PRIMARY KEY,
    batch_id                BIGINT       NOT NULL REFERENCES pj_import_batch(id),
    record_type             VARCHAR(20)  NOT NULL,  -- SIGNED/NEW_SIGN/ATTENDANCE/POINTS/MANUAL
    period                  VARCHAR(7),
    employee_id             BIGINT,
    employee_external_code  VARCHAR(64),
    source_key              VARCHAR(255),
    biz_type                VARCHAR(64),
    receivable_amount       NUMERIC(18,2),
    received_amount         NUMERIC(18,2),
    share_ratio             NUMERIC(10,4),
    role_type               VARCHAR(64),
    extra_json              JSONB,
    raw_data_id             BIGINT,
    validation_status       SMALLINT     DEFAULT 0,
    validation_msg          VARCHAR(1000),
    create_time             TIMESTAMP    DEFAULT CURRENT_TIMESTAMP
);

CREATE UNIQUE INDEX uk_norm_source_key
    ON pj_normalized_record(batch_id, source_key)
    WHERE source_key IS NOT NULL;
```

### 6.5 模板种子（5 类，无 EMPLOYEE）

```sql
INSERT INTO pj_import_template (source_type, template_version, column_mapping, enabled)
VALUES ('KE_SIGNED','V100',...), ('KE_NEW_SIGN','V100',...),
       ('ATTENDANCE','V100',...), ('POINTS','V100',...), ('OTHERS','V100',...);
```

### 6.6 文件生命周期

归档/废弃批次的原文**永久保留、禁止删除**；RawData 只读，禁止物理删除。

---

## 七、API 设计

| 方法 | 路径 | 说明 |
|---|---|---|
| POST | `/api/import/upload` | 上传单据 Excel（**不含员工**），创建批次 |
| POST | `/api/import/manual` | 手工录入（OTHERS） |
| GET | `/api/import/batches` | 批次列表（过滤 SUPERSEDED） |
| GET | `/api/import/batches/{id}` | 批次详情 + ImportIssue |
| POST | `/api/import/batches/{id}/renormalize` | 重归一化 |
| POST | `/api/import/batches/{id}/archive` | 归档（可部分放行） |
| POST | `/api/import/issues/{id}/ignore` | 标记 IGNORED（算薪人员/总监） |

> 员工导入接口在 people 域：`POST /api/people/employee/import`。

---

## 八、Port / 事件

### 8.1 对外查询 Port

```java
public interface NormalizedRecordQueryPort {
    /** 默认实现强制内置 SUPERSEDED 过滤：
     *  archive_status = ARCHIVED AND superseded_by_batch_id IS NULL */
    List<NormalizedRecord> query(QueryCriteria criteria);
}
```

### 8.2 对 people 的依赖（★ V2.0 降级为只读）

```java
// 仅此一个 Port，来自 panjia-people-api
public interface EmployeeQueryPort {
    Long resolveIdByCode(String employeeExternalCode);  // 工号/系统号 → employee_id
    boolean exists(Long employeeId);
}
```

**禁止项（CI 拦截）：** 写 `pj_people_*` 表；引用 `EmployeeImportSink` / `EmployeeWritePort`；引入 people `domain` 包。

### 8.3 事件范围

| 事件 | 触发 | 说明 |
|---|---|---|
| `import.parsed` | 事务 A 完成 | 内部驱动归一化 |
| `import.archived` | 批次归档 | 供 performance 等消费 |
| `import.superseded` | — | **V1 不发布**（下游自行过滤） |

**V2.0 明确：员工导入不产生任何本域事件**（员工导入归 people 域，其状态机与事件均在本域之外）。

### 8.4 事件表归属

`pj_event_outbox` 归属公共底座统一管理，本域不建表，仅按规约写入。

---

## 九、编码任务清单

| 任务 | 内容 | 关键点 |
|---|---|---|
| Task-2-1 | `ImportBatch` 聚合根 + 状态机 | `FAILED`/`ARCHIVED` 空 next，单测固化 |
| Task-2-2 | `ImportBatchMapper` + SUPERSEDED 过滤 | 唯一索引 + 过滤单测 |
| Task-2-3 | RawData 5 张分表 Mapper（**无 employee**） | insert-only |
| Task-2-4 | `NormalizedRecordMapper` | `uk_norm_source_key` |
| Task-2-5 | `SourceKeyGenerator` SPI | 订单号/合同号，样本到位前接口先行 |
| Task-2-6 | `ManualEntryDataSource` | 手工去重判重键 + 三处同步 + 单测 |
| Task-2-7 | `KeSignedDataSource` | 结佣列映射 |
| Task-2-8 | `KeNewSignDataSource` | 新签列映射 |
| Task-2-9 | `AttendanceDataSource` | 考勤列映射 |
| Task-2-10 | `PointsDataSource` | 积分列映射 |
| Task-2-11 | **接入 `common-import-util`** | 用 `FileArchiver`/`FileParser`/`TemplateResolver`/`BasicValidator` 替换自研解析 |
| Task-2-12 | `TemplateEngine` | 模板驱动列映射 |
| Task-2-13 | AuditPort 全量留痕 | 重归一化/IGNORED 审计 |
| Task-2-14 | `ImportEngine` 两段事务编排 | 事务 A / 事务 B |
| Task-2-15 | `NormalizedRecordQueryPort` 实现 | 内置 SUPERSEDED 过滤 |
| Task-2-16 | Controller + 权限拦截 | IGNORED 鉴权 |
| Task-2-17 | **CI：断言不出现 `pj_people_` / `EmployeeImportSink`** | V2.0 新增，守护依赖降级 |

> **V2.0 移除的任务**（原员工导入相关）：`EmployeeDataSource`、`EmployeeImportSink` 实现、部门自动建、岗位角色匹配、员工物理清理、员工事件开关 —— 全部回归 people 域（见《员工域详细设计 V6.0》Task-1-9~1-11）。

---

## 十、架构决策记录（ADR）

| 编号 | 决策 | 结论 |
|---|---|---|
| IMP-001 | 导入域边界 | 单据 Excel → RawData → NormalizedRecord，不构造业务事实 |
| IMP-002 | RawData 分表 | 按 source_type 分表 |
| IMP-003 | RawData 只读 | insert-only + AuditPort，禁触发器 |
| IMP-004 | 重复导入 | 单据逻辑失效（RawData 保留 + SUPERSEDED 过滤） |
| IMP-005 | 金额处理 | 只做类型转换，折算下沉 performance |
| IMP-011 | Raw 静态分表 | 新增 source_type 属版本迭代变更 |
| IMP-012 | 模板激活 | 同 source_type 单套激活模板 |
| **IMP-013（废止）** | ~~EMPLOYEE 融合~~ | **V2.0 废止** —— 员工导入回归 people 域，本域仅单据 |
| **IMP-014（废止）** | ~~主数据物理清理~~ | **V2.0 废止** —— 员工主数据不物理删，由 people 域走变更入口演进 |
| **IMP-015（新增）** | **工具层与业务层分离** | 解析/模板/归档/基础校验下沉 `common-import-util`；两个业务域**仅工具层复用、业务层不复用** |
| **IMP-016（新增）** | **对 people 仅只读** | 本域只依赖 `EmployeeQueryPort`；禁止写 `pj_people_*`；守护 V1.8 ADR-007/021/022 |

---

## 十一、风险与待确认

| 项 | 说明 | 处理 |
|----|------|------|
| `source_key` 完整映射 | 租赁轻托管/写字楼租赁等细分待样本 | **待贝壳真实 Excel 样本到位后固化**（Task-2-5） |
| 跨月检测 | V1 固化为命中即 FAILED | 是否配置豁免待实施确认 |
| IGNORED 权限 | 仅算薪人员/总监 | 角色编码待 RuoYi 角色体系确认 |
| 大事务 | 单批 > 5000 行 | 分批 300 + 两段事务 |
| 监控/告警/限流 | V1 暂不做 | 记录风险 |

---

## 附录 A：V1.4 → V2.0 变更清单

| 项 | V1.4 | V2.0 |
|----|------|------|
| `EMPLOYEE` source_type | 第 6 类（在本域） | **移除**，归 people 域 |
| `pj_import_raw_employee` | 有 | **移除** |
| `EmployeeImportSink` / `EmployeeDataSource` | 有（受控写） | **移除** |
| 对 people 依赖 | 读 + 受控写 | **仅只读** `EmployeeQueryPort` |
| 员工导入事务 | 两段式（A 落 Raw / B Sink） | 归 people 域（阶段 A 诊断 + 阶段 B 单一大原子事务） |
| 解析能力 | 自研 | **下沉 `common-import-util`** |
| source_type 数量 | 6 | **5** |
| ADR | IMP-013/014（融合/物理清理） | **废止**；新增 IMP-015/016 |

## 附录 B：与 people 域导入的分工速查

| 维度 | `panjia-import`（本域） | `panjia-people` |
|------|------------------------|-----------------|
| 导入对象 | 业绩/考勤/积分/费用单据 | 员工主数据 |
| 批次表 | `pj_import_batch` | `pj_people_import_batch` |
| 原始表 | `pj_import_raw_*`（5 张） | `pj_people_import_raw` |
| 问题表 | `pj_import_issue` | `pj_people_import_issue` |
| 模板表 | `pj_import_template` | `pj_people_import_template` |
| 状态机 | PARSING→NORMALIZING→PENDING_CONFIRM→ARCHIVED | PARSING→VALIDATING→IMPORTING→SUCCESS |
| 重复导入 | 逻辑失效（SUPERSEDED 过滤） | 演进（upsert + 变更入口，不删） |
| 工具层 | `common-import-util` | `common-import-util` |
| 业务层复用 | **不复用** | **不复用** |

---

**全文完 · V2.0 落地版（单据域回归）**
