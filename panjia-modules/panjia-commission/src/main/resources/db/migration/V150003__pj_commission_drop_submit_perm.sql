-- ============================================================================
-- V150003 结佣申请「提交」按钮权限清理
-- 背景：结佣发起与提交合并为一个接口（POST /commission/apply 一次完成发起+提交，
--       并自动处理驳回单重提），不再单独提供 POST /commission/apply/{id}/submit。
--       后端 @SaCheckPermission("commission:apply:submit") 已随接口删除，
--       对应 F 型按钮菜单（menu_id=1761400000000011822「提交申请」）及
--       各角色授权（超管/财务/总监/店长）同步清理，避免悬空权限码。
-- ============================================================================

BEGIN;

-- 1. 删除各角色对该按钮的授权（先删 role_menu，避免悬空引用）
DELETE FROM sys_role_menu WHERE menu_id = 1761400000000011822;

-- 2. 删除「提交申请」F 型按钮菜单
DELETE FROM sys_menu WHERE menu_id = 1761400000000011822;

COMMIT;
